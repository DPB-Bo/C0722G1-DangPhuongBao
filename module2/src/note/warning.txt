dùng next int thì bị nuốt mất các khai báo sau nên dùng là kiểu dữ liệu của biến -> parse kdl -> next line
protected thì cùng package và class con có thể truy cập
default thì cùng package nhung class con không thể truy cập
private thi truy cập bằng niềm tin
///////////////////////////
ý nghĩa của static:

    + Gọi static qua class
    + Hắn được dùng khi mà có thuộc tính trùng cho toàn bộ đối tượng của class
    + static chỉ chơi được với static, muốn gọi non-static trong phương thức static phải gọi qua đối tượng
    + muốn gọi static trong non-static phải gọi qua class